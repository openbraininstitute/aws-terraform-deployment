Content-Type: multipart/mixed; boundary="==BOUNDARY=="
MIME-Version: 1.0

--==BOUNDARY==
Content-Type: text/cloud-config; charset="us-ascii"

#cloud-config

users:
    - name: obiuser
      uid: 4000
      ssh_authorized_keys:
        - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBCRTRSpJMLRRk0GuIcQ/OU5fGwgX0YhIMsy/sSgdzQc
      shell: /bin/bash
#    - name: mgevaert
#      uid: 1001
#      ssh_authorized_keys:
#        - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBCRTRSpJMLRRk0GuIcQ/OU5fGwgX0YhIMsy/sSgdzQc
#      sudo: ALL=(ALL) NOPASSWD:ALL
#      shell: /bin/bash

packages:
  # may need `sudo amazon-linux-extras install lustre` on al2
  - lustre-client

runcmd:
  - echo "========== Setting up FSX mount =========="
  - mkdir -p /data
  - grep -q "${fsx_dns_name}" /etc/fstab || echo "${fsx_dns_name}@tcp:/${fsx_mount_name} /data lustre defaults,_netdev 0 0" >> /etc/fstab
  - mount /data
  - cd /tmp
#  - echo "========== Install EFA =========="
#  - curl -O https://efa-installer.amazonaws.com/aws-efa-installer-latest.tar.gz
#  - tar xf aws-efa-installer-latest.tar.gz && cd aws-efa-installer
#  - ./efa_installer.sh -y --skip-kmod --mpi=openmpi5
#  - echo "========== Install aws-pcs-slurm-25.05-installer-latest.tar.gz =========="
#  - cd /tmp
#  - curl -O https://aws-pcs-repo-${region}.s3.${region}.amazonaws.com/aws-pcs-slurm/aws-pcs-slurm-25.05-installer-latest.tar.gz
#  - tar xf aws-pcs-slurm-25.05-installer-latest.tar.gz && cd aws-pcs-slurm-25.05-installer/
#  - ./installer.sh -y
#  - echo "========== Install aws-pcs-agent-v1-latest.tar.gz =========="
#  - cd /tmp
#  - curl -O https://aws-pcs-repo-${region}.s3.${region}.amazonaws.com/aws-pcs-agent/aws-pcs-agent-v1-latest.tar.gz
#  - tar xf aws-pcs-agent-v1-latest.tar.gz && cd aws-pcs-agent
#  - ./installer.sh

--==BOUNDARY==--
