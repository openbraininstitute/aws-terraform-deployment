The goal is to provide a bunch of alarms based on metrics, which can be
applied to most ECS containers on the platform.

The required metrics are only enabled when Container Insights is set to 'enhanced'.
